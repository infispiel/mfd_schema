[project]
name = "mfd-schema"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "lark>=1.2.2",
    "pytest>=8.3.3",
    "ruff>=0.6.5",
]

[tool.uv]
cache-dir = "./.uv_cache"
